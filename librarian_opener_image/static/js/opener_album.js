// Generated by CoffeeScript 1.10.0
(function(window, $) {
  'use strict';
  var albumItem, container, openImage;
  albumItem = $('.album-item');
  container = $('.opener-main');
  openImage = function(e) {
    var res, url;
    e.preventDefault();
    url = $(this).find('a').attr('href');
    res = $.get(url);
    return res.done(function(data) {
      return container.replaceWith(data);
    });
  };
  return albumItem.click(openImage);
})(this, this.jQuery);
